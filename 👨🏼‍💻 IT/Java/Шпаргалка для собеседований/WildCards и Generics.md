#### Принцип PECS (Producer Extends, Consumer Super)
- если коллекция **отдаёт** элементы (только читаем из неё) → используй `<? extends T>`;
- если коллекция **принимает** элементы (только пишем в неё) → используй `<? super T>`.